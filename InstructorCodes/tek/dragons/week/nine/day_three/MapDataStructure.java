package tek.dragons.week.nine.day_three;

public class MapDataStructure {
	/*
	 * Maps are a data structure base on Key Value pairs. 
	 * Map is interface. and most popular Classes that implement map are
	 *  - maps are generic, for both key and value we need non-primitive data type. 
	 *  - keys are based on Set, 
	 *  - no duplicate key accepted. 
	 *  - if a key is duplicate new value will override with old value. 
	 *  - in order to retrieve value we have to know the key
	 * 1) HashMap
	 * - Keys in HashMap base on HashSet. 
	 * 2) TreeMap
	 * - Keys in TreeMap base on TreeSet.
	 */
}
