package tek.dragons.week.three.day_one;

public class AssignmentOperators {
	public static void main(String[] args) {
		// = is assignment operator. 
		// the left side of the assignment is called declaration 
		// the right side fo the assignment is assign value. 
		
		int number1 = 10; 
		int number2 = 20;
		
		// in assignment java first run the right side of = operator. 
		// then assign it to the variable. 
		int result = number1 + number2; 
		
		String name = "mohammad";
		String newName = name.toUpperCase();
		
		//Variable declaration only. 
		int result2; 
		//Assignment value to a already declared variable. 
		result2 = 20 * 20; 
		
	}
}
